# syntax = docker/dockerfile:1
# Use the official MongoDB image as the base image
FROM mongo:%%MONGO_VERSION%%

ARG MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE:-mongo} \
    MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME:-aerius} \
    MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD:-aerius} \
    MI_DATABASE_VERSION=${MI_DATABASE_VERSION} \
    MI_INPUT_FILE=${MI_INPUT_FILE} \
    MI_RUN_SCRIPT_FOLDER=${MI_RUN_SCRIPT_FOLDER} \
    MI_DUMP_DATABASE=${MI_DUMP_DATABASE:-false} \
    MI_INITIALIZE_ON_BUILD=${MI_INITIALIZE_ON_BUILD:-false} \
    MI_NEXUS_BASE_URL=${MI_NEXUS_BASE_URL} \
    MI_NEXUS_REPOSITORY=${MI_NEXUS_REPOSITORY} \
    MI_BIN_FOLDER=/mi/bin \
    MI_SOURCE_FOLDER=/mi/source \
    MI_DBDATA_FOLDER=/mi/dbdata \
    MI_DUMP_FOLDER=/mi/dump \
    MI_SKIP_DBDATA_SYNC=${MI_SKIP_DBDATA_SYNC:-false} \
    MI_BIN_FOLDER_CLEANUP=${MI_BIN_FOLDER_CLEANUP:-false} \
    MI_SOURCE_FOLDER_CLEANUP=${MI_SOURCE_FOLDER_CLEANUP:-false} \
    MI_DBDATA_FOLDER_CLEANUP=${MI_DBDATA_FOLDER_CLEANUP:-false} \
    MI_SKIP_UNSET_ENVS=${MI_SKIP_UNSET_ENVS:-false} \
    MI_ENTRYPOINT_WIPE_DB_ON_CHANGES=${MI_ENTRYPOINT_WIPE_DB_ON_CHANGES:-false}

ENV MONGO_INITDB_DATABASE=${MONGO_INITDB_DATABASE} \
    MONGO_INITDB_ROOT_USERNAME=${MONGO_INITDB_ROOT_USERNAME} \
    MONGO_INITDB_ROOT_PASSWORD=${MONGO_INITDB_ROOT_PASSWORD} \
    MI_DATABASE_VERSION=${MI_DATABASE_VERSION} \
    MI_INPUT_FILE=${MI_INPUT_FILE} \
    MI_RUN_SCRIPT_FOLDER=${MI_RUN_SCRIPT_FOLDER} \
    MI_DUMP_DATABASE=${MI_DUMP_DATABASE} \
    MI_INITIALIZE_ON_BUILD=${MI_INITIALIZE_ON_BUILD} \
    MI_NEXUS_BASE_URL=${MI_NEXUS_BASE_URL} \
    MI_NEXUS_REPOSITORY=${MI_NEXUS_REPOSITORY} \
    MI_BIN_FOLDER=${MI_BIN_FOLDER} \
    MI_SOURCE_FOLDER=${MI_SOURCE_FOLDER} \
    MI_DBDATA_FOLDER=${MI_DBDATA_FOLDER} \
    MI_DUMP_FOLDER=${MI_DUMP_FOLDER} \
    MI_SKIP_DBDATA_SYNC=${MI_SKIP_DBDATA_SYNC} \
    MI_BIN_FOLDER_CLEANUP=${MI_BIN_FOLDER_CLEANUP} \
    MI_SOURCE_FOLDER_CLEANUP=${MI_SOURCE_FOLDER_CLEANUP} \
    MI_DBDATA_FOLDER_CLEANUP=${MI_DBDATA_FOLDER_CLEANUP} \
    MI_SKIP_UNSET_ENVS=${MI_SKIP_UNSET_ENVS} \
    MI_ENTRYPOINT_WIPE_DB_ON_CHANGES=${MI_ENTRYPOINT_WIPE_DB_ON_CHANGES}

# Install required packages
RUN apt-get update \
    && apt-get install -y wget curl jq \
    && rm -rf /var/lib/apt/lists/* \
    # Mongo-Initializer root folder for all scripts, sources and dbdata-files.
    && mkdir /mi \
    && chown mongodb:mongodb /mi

# Copy all necessary scripts
COPY docker/mongo-initializr.sh /docker-entrypoint-initdb.d/
COPY bin/ "${MI_BIN_FOLDER}"
COPY docker/docker-mi-entrypoint.sh /usr/local/bin/

# Set up our own entrypoint (CMD gets reset for security reasons, so let's add that as well)
ENTRYPOINT ["docker-mi-entrypoint.sh"]
CMD ["mongod"]
